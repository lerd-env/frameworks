package: symfony/beanstalkd-messenger
frameworks:
  - name: symfony

# The project talks to this service through the package, so the wizard ticks it.
suggest_services:
  - name: beanstalkd
    reason: Beanstalkd for this Messenger transport
