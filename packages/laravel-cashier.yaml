package: laravel/cashier
frameworks:
  - name: laravel

# The project talks to this service through the package, so the wizard ticks it.
suggest_services:
  - name: stripe-mock
    reason: Test Cashier billing without reaching Stripe
