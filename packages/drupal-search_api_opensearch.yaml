package: drupal/search_api_opensearch
frameworks:
  - name: drupal

# The project talks to this service through the package, so the wizard ticks it.
suggest_services:
  - name: opensearch
    reason: Search API backend for this module
