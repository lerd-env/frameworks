package: symfony/redis-messenger
frameworks:
  - name: symfony

# Alternatives, most important first: lerd puts forward the first one this
# machine already runs, else the first, and none when the project uses one.
suggest_services:
  - name: redis
    reason: Redis for this Messenger transport
  - name: valkey
    reason: Redis-compatible Messenger transport
