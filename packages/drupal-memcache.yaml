package: drupal/memcache
frameworks:
  - name: drupal

# The project talks to this service through the package, so the wizard ticks it.
suggest_services:
  - name: memcached
    reason: Cache backend for the Memcache module
