package: vladimir-yuldashev/laravel-queue-rabbitmq
frameworks:
  - name: laravel

# The project talks to this service through the package, so the wizard ticks it.
suggest_services:
  - name: rabbitmq
    reason: RabbitMQ for this queue driver
