package: pusher/pusher-php-server

# The project talks to this service through the package, so the wizard ticks it.
suggest_services:
  - name: soketi
    reason: Pusher-compatible websockets without Pusher
