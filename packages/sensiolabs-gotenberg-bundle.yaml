package: sensiolabs/gotenberg-bundle
frameworks:
  - name: symfony

# The project talks to this service through the package, so the wizard ticks it.
suggest_services:
  - name: gotenberg
    reason: Renders PDFs locally for this bundle
