package: drupal/redis
frameworks:
  - name: drupal

# Alternatives, most important first: lerd puts forward the first one this
# machine already runs, else the first, and none when the project uses one.
suggest_services:
  - name: redis
    reason: Cache backend for the Redis module
  - name: valkey
    reason: Redis-compatible cache backend
