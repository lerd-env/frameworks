package: doctrine/mongodb-odm-bundle
frameworks:
  - name: symfony

# The project talks to this service through the package, so the wizard ticks it.
suggest_services:
  - name: mongo
    reason: MongoDB for Doctrine ODM
